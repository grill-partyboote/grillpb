import React from "react";
import s from "./slider.pcss";
interface HeaderProps {

}

export function Slider(props: HeaderProps): JSX.Element {
	return (
		<div className={ s.slider }>
			slider
		</div>)
};
